import React, { Component } from "react";
import { Text, View } from "react-native";

export default class SampleComponent extends Component {
  render() {
    return (
      <View>
        <Text> 절대경로 import </Text>
      </View>
    );
  }
}
